defmodule Buckets do
  # Produces n buckets, evenly spaced between dates startDate and endDate, each bucket has a min-max value and a min-max date
  # Input series is expected to be [{date, value}, ...]
  # Interpolation is last-point.
  def bucket(series, n, startDate, endDate) do
  end
end
